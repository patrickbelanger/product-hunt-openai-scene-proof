sceneproof:
  astra:
    api-key: ''
  http:
    reads-per-minute: 10000
    writes-per-minute: 10000
    media-per-minute: 10000
  paid-runs:
    hourly-limit: 10000
    daily-limit: 10000
spring:
  datasource:
    url: ${TEST_DATABASE_URL:jdbc:postgresql://localhost:55432/sceneproof?currentSchema=sceneproof_test}
  flyway:
    default-schema: sceneproof_test
    schemas: sceneproof_test
  jpa:
    properties:
      hibernate:
        default_schema: sceneproof_test
